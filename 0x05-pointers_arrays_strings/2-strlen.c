#include "main.h"
#include <stdio.h>

/**
 * _strlen - output the lenght on a string
 * @s: the string to be checked
 * Return: Always return c
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{
		continue;
	}

	return (c);
}
